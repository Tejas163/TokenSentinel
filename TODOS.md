# TODOS

Generated by /plan-ceo-review on 2026-06-01

## ✅ Priority 1: Foundation Fixes (completed)

- [x] **Go router tests** — route resolution, provider selection, retry, circuit breaker
- [x] **Observability** — `/metrics` endpoint, structured logging, health endpoint for cost dashboard
- [x] **Cost data durability** — buffer failed cost writes (channel + retry), pubsub reconnect in cost dashboard
- [x] **Erlang `write_cost`** — return `{ok, Response}` instead of silent `ok` on Redis failure
- [x] **Auth** — API key on Go router and cost dashboard, Redis requirepass
- [x] **Data retention** — auto-prune cost entries older than 90 days (configurable)
- [x] **Client header leak** — strip sensitive headers before forwarding to upstream
- [x] **CI** — Go version 1.26 is correct for installed 1.26.1 (non-issue)
- [x] **Rust body size limit** — cap at 10MB
- [x] **Token estimation** — prompt token counting for cost-estimation-before-routing

## ✅ Spring AI SDK (completed)

- [x] **REST clients** — TokenCostClient, AdminClient with retry + auth
- [x] **Missing DTOs** — AnomalyEntry, Team, BudgetRule, BudgetStatus
- [x] **Spring AI tools** — `TokenSentinelAiTools` with 10 `@Tool` methods (cost, anomalies, teams, budget rules)
- [x] **Auto-configuration** — `MethodToolCallbackProvider` registers all tools as `ToolCallback` beans
- [x] **Metrics** — admin counter tags, latency timer, token distribution
- [x] **Tests** — 21 unit tests (REST clients + tool annotation/reflection)

## ✅ Scope Expansions (completed)

- [x] **Anomaly detection** — 3σ sliding window per-model, SSE broadcast, `/api/dashboard/anomalies` endpoint
- [x] **Real-time SSE dashboard** — live cost + anomaly events streamed to browser, `LIVE` badge
- [x] **Budget threshold webhooks** — `budget_rules` table, admin CRUD, 5-min checker, 30-min cooldown
- [x] **Autonomous model selection** — prompt length heuristic maps to cheap/medium/capable tier, 20+ models
- [x] **Budget-aware routing** — per-team budgets via X-Team-Name, Redis counters, auto-reroute to cheapest
- [x] **Cost annotations** — `X-Model-Used` and `X-Cost-Cents` on every response
- [x] **In-memory route cache** — `sync.Map` with 60s TTL, Redis fallback on miss
- [x] **Slack/Teams digest** — configurable webhook + schedule, per-model breakdown with attachment format
