# TODOS

Generated by /plan-ceo-review on 2026-06-01

## Priority 1: Foundation Fixes (next iteration)

- [ ] **Go router tests** — route resolution, provider selection, retry, circuit breaker
- [ ] **Observability** — `/metrics` endpoint, structured logging, health endpoint for cost dashboard
- [ ] **Cost data durability** — buffer failed cost writes (channel + retry), pubsub reconnect in cost dashboard
- [ ] **Erlang `write_cost`** — return `{ok, Response}` instead of silent `ok` on Redis failure
- [ ] **Auth** — API key on Go router and cost dashboard, Redis requirepass
- [ ] **Data retention** — auto-prune cost entries older than 90 days (configurable)
- [ ] **Client header leak** — strip sensitive headers before forwarding to upstream
- [ ] **CI** — fix Go version from 1.26 to 1.24
- [ ] **Rust body size limit** — cap at 10MB
- [ ] **Token estimation** — prompt token counting for cost-estimation-before-routing

## Deferred Scope Expansions

- [ ] Budget-aware routing with per-team monthly budgets + admin API
- [ ] Response header cost annotations (X-Cost-Cents, X-Model-Used)
- [ ] Anomaly detection with 3σ sliding window + configurable auto-action
- [ ] Automated Slack/Teams cost digest (daily/weekly)
- [ ] Local in-memory route cache in Go router (Redis fallback)
- [ ] Autonomous model selection (two-tier heuristic + classifier sidecar)
- [ ] Real-time SSE dashboard (nice polish, not core)
- [ ] Budget threshold webhooks (complement, low urgency)
